<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="user">
	
	<select id="viewUser" parameterType="UserVO" resultType="UserVO">
		SELECT ID, PW, NAME, HIREDATE
		FROM USERTABLE
	</select>
	
	<insert id="insertUser" parameterType="UserVO">
		INSERT INTO USERTABLE (ID, PW, HIREDATE, NAME) VALUES (#{id}, #{pw}, SYSDATE, #{name})
	</insert>
	
	<delete id="deleteUser" parameterType="UserVO">
		DELETE FROM UserTable WHERE id = #{id}
	</delete>
	
	<update id="updateUser" parameterType="UserVO">
		UPDATE USERTABLE 
		SET PW = #{pw}, NAME = #{name}
	</update>
	
	<select id="getUserForm" parameterType="String" resultType="UserVO">
		SELECT ID, PW, NAME, HIREDATE
		FROM USERTABLE
		WHERE ID = #{id}
	</select>
	
	
	<!-- 로그인 -->
	<select id="login" resultType="CustomUserDetails">
		SELECT *
		FROM USERTABLE
		WHERE ID = #{id}
	</select>
	
</mapper>
